# ── Core (required) ──────────────────────────────
# All versions pinned to the known-good environment (2026-06-04).
# To upgrade a package: test locally, update the pin here, then deploy.
pydantic==2.13.4
typer==0.9.0
python-dotenv==1.2.2
litellm==1.87.0
openai==2.40.0
pymupdf==1.27.2.3
beautifulsoup4==4.12.3
supabase==2.30.1
psycopg2-binary==2.9.12
langfuse==2.60.10
redis==7.4.0
playwright==1.60.0
fastapi==0.136.1
uvicorn==0.47.0
httpx==0.28.1
anyio==4.13.0
huggingface-hub==1.5.0
tokenizers==0.22.2

# ── AI / ML (pulls in PyTorch — heavy but needed) ─
sentence-transformers==5.5.1
scikit-learn==1.7.1
easyocr==1.7.2

# ── Dev / Test (not shipped in the production Docker image) ────────────
# Installed by CI and local devs. The Dockerfile skips these via a
# separate pip install step that only targets the lines above this block.
pytest==8.3.5
